import { FiltrofacultadPipe } from './filtrofacultad.pipe';

describe('FiltrofacultadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrofacultadPipe();
    expect(pipe).toBeTruthy();
  });
});
